{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sass": "sass scss:css --watch",
    "sass:build": "sass scss:css --no-source-map --style=compressed"
  },
  "devDependencies": {
    "sass": "^1.97.3"
  }
}
